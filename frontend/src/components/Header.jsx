import React from 'react';

const Header = () => {
  return (
    <header className="app-header">
      <h2 className="brand-title">Easy URL Shortener</h2>
    </header>
  );
};

export default Header;
